# Work repos

Client and work projects. Still under the `bygama` account, split from `mine.md` so
personal and paid work don't share a folder. Cloned and nothing else happens to them —
no symlinks, no per-project setup.

They land in `repos\work\` because **this file is called `work.md`**. See `README.md`.

`Ecommerce-RCSistemas` also lives in `repos\work\` but is not in the list: it has no
GitHub remote yet, so there is nothing to clone.

## The list

| Repo            | Remote                   |
| --------------- | ------------------------ |
| Bystellabotella | `bygama/Bystellabotella` |
| KioscoDiagonal  | `bygama/Kiosco-Diagonal` |
| RCSistemas      | `bygama/RCSistemas`      |
| ecotronk        | `bygama/ecotronk`        |
