# Post-restore record — 30/07/2026

Benchmark captured after the full restore of this repository on Windows 11 Pro build
26200 (machine ATLAS, local passwordless account), so the next format has a number to
compare against. Historical record, not desired state: nothing reads it back.

Measured after the final reboot, with the Claude Code session open and its own
processes subtracted.

| Metric | Stock (00:08) | Final gross | Final net (without Claude) |
| --- | --- | --- | --- |
| Processes | 224 | 184 | **151** |
| Claude session overhead | — | 33 processes / 2.8 GB | subtracted |
| Running services | 111 | 113 | 113 |
| Current-user AppX packages | 126 | 103 | 103 |
| Managed registry compliant | 8/45 | 49/49 | 49/49 |
| Power | not Balanced | Balanced, AC sleep 0 s | idem |
| Debloat remaining | 23 AppX | 0 AppX · 0 Win32 | idem |
| Free memory | 20.5 GB | 24.4 GB | — |

Conditions: startup launchers disabled in Task Manager (Discord, Docker Desktop, Riot,
Steam), Edge closed, privacy services settled (DiagTrack, per-user CDP, geolocation,
maps, phone, biometrics, WAP push all Stopped + Disabled). Installed and active:
Docker/WSL plumbing, Tailscale, G HUB, Wallpaper Engine, TranslucentTB, NVIDIA App.

Known accepted residue: NVIDIA Overlay ×5 (~510 MB; can be turned off in NVIDIA App when
not recording) and one CrossDeviceResume process (harmless, its backing service is
disabled).

Source captures: `2026-07-30_000818-stock.json` and `2026-07-30_020736-optimized.json`
under `%LOCALAPPDATA%\workstation-audits` on the machine that was measured. Audit JSON
stays local by design; this file only preserves the comparison that made the restore
verifiable.
