name: verify

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  powershell:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run repository verification
        shell: pwsh
        run: ./tests/run.ps1
